{
  "dependencies": {
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "pg": "^8.11.3"
  },
  "scripts": {
    "start": "node server.js",
    "watch": "nodemon",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "name": "pokedex_app",
  "version": "1.0.0",
  "private": "true",
  "description": "",
  "main": "server.js",
  "devDependencies": {
    "nodemon": "^3.0.1"
  }
}
